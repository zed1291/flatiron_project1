CREATE TABLE favourites (
    id INTEGER PRIMARY KEY,
    city_id INTEGER,
    user_id INTEGER
);

CREATE TABLE cities (
    id INTEGER PRIMARY KEY,
    name TEXT
);

CREATE TABLE users (
    id INTEGER PRIMARY KEY,
    name TEXT
);